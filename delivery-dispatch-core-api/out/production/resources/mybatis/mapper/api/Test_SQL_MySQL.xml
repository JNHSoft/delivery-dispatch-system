<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.cntt.core.config.api.TestMapper">

	<!-- TEST -->
	<select id="selectTestQuery" resultType="String">
		SELECT name AS branchName FROM branch limit 1
	</select>

</mapper>
 