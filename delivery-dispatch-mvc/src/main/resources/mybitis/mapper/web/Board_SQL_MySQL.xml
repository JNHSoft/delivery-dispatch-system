<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.cntt.core.mapper.BoardMapper">

	<!-- selectBoardList -->
	<select id="selectBoardList" parameterType="kr.co.cntt.core.model.web.Board" resultType="Board">
		SELECT
			id, branch_id, title, content, to_rider, to_store, to_branch,
			deleted, created_datetime, modified_datetime
		FROM notice
		WHERE 1=1
		<choose>
			<when test='searchType != null and searchType.equals("title")'>
				AND title LIKE '%#{searchWord}%'
			</when>
			<when test='searchType != null and searchType.equals("content")'>
				AND content LIKE '%#{searchWord}%'
			</when>
			<otherwise>
				AND (title LIKE '%#{searchWord}%' OR content LIKE '%#{searchWord}%')
			</otherwise>
		</choose>
	</select>


</mapper>
 